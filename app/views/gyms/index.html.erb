<p id="notice"><%= notice %></p>

<body bgcolor="#D6EAF8">
  
</body>
<h1 align="center">Workouts</h1>

<table  cellpadding="10", align="center" >
  <thead>
    <tr bgcolor="##1ABC9C">
      <th>Date</th>
      <th>Workout</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody bgcolor= "#EBDEF0", align="center">
    <% @gyms.each do |gym| %>
      <tr align="center">
        <td><%= gym.Date %></td>
        <td><%= gym.Workout %></td>
        <td><%= link_to 'Show', gym %></td>
        <td><%= link_to 'Edit', edit_gym_path(gym) %></td>
        <td><%= link_to 'Destroy', gym, method: :delete, data: { confirm: 'Are you sure?'} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p id ="new_work", align="center"><%= link_to 'New Workout', new_gym_path %>
